loss:
  - CrossEntropyLoss
networks:
  # mixed models
  - - { layer_type: convolution ,
        bias: False,
        labels: [
          { label_type: primary}
        ],
        properties: [
          { name: distances, values: [ 0, 50 ] }
        ]
      }
    - { layer_type: aggregation,
        bias: True,
        labels: [
          { label_type: primary }
        ]
    }
